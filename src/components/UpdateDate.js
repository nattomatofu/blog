function updateDate({ children }) {
    return (
        <div className="mt-4 text-right">
            <p className="font-thin">更新日 : {children}</p>
        </div>
    );
}

export default updateDate;
